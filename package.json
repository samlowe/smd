{
  "name": "smd",
  "version": "0.3.20",
  "description": "Simple Markdown Viewer",
  "license": "MIT",
  "private": true,
  "scripts": {
    "tauri": "cargo tauri",
    "test": "node --test ui/tests/*.test.js && cargo test -p smd-core --manifest-path src-tauri/Cargo.toml",
    "test:js": "node --test ui/tests/*.test.js",
    "test:rust": "cargo test -p smd-core --manifest-path src-tauri/Cargo.toml"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/plugin-dialog": "^2.6.0",
    "@tauri-apps/plugin-shell": "^2.3.5",
    "highlight.js": "^11.11.1",
    "mermaid": "^11.12.3"
  }
}
